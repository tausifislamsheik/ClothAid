import { Helmet } from "react-helmet";

const HowToHelp = () => {
    return (
        <div>
            <Helmet>
                <title>How to Help || ClothAid</title>
            </Helmet>
        </div>
    );
};

export default HowToHelp;